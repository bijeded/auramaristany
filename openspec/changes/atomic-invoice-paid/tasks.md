# Tasks

## 1. Migration

- [ ] 1.1 Write `supabase/migrations/023_atomic_invoice_paid.sql` with `record_invoice_and_advance(...)` per design D2: `security invoker`, `set search_path = public, pg_temp`, invoice insert `on conflict (stripe_invoice_id) do nothing` returning false on no insert, `p_months_elapsed = p_expected_months_elapsed + 1` guard, all-or-none content triple, `p_complete` writing `completed_at = now()` and `cancel_at_period_end = true` together, guarded update raising `serialization_failure` unless exactly one row changed, `comment on function`; `revoke execute … from public, anon, authenticated` and `grant execute … to service_role` (D3); block-comment header only, no `--`. Verify: it is the next number after 022 and `git diff --stat supabase/migrations` shows only the new file.
- [ ] 1.2 Apply 023 to project `bgvxaagfnzvzamtxqbkg` via the Management API (SQL on one line), then `notify pgrst, 'reload schema'`. Verify (rule 11): `select proname, prosecdef from pg_proc where proname = 'record_invoice_and_advance'` returns one row with `prosecdef = false`, and `select grantee from information_schema.routine_privileges where routine_name = 'record_invoice_and_advance'` lists `service_role` and neither `anon` nor `authenticated`.
- [ ] 1.3 Verify atomicity against the real DB, non-destructively — every call inside `begin; … rollback;`, on one existing seeded subscription whose current `months_elapsed` / `content_ordinal` are read first, with a fake `stripe_invoice_id` (`in_verify_d16_*`): (a) a call with a wrong `p_expected_content_ordinal` raises and `select count(*) from invoices where stripe_invoice_id = …` is 0 afterwards in the same transaction; (b) a correct call returns true, the invoice row exists and the subscription advanced one step; (c) repeating (b) returns false and changes nothing; (d) `p_months_elapsed = expected + 2` raises. After `rollback`, confirm the subscription row and invoice count equal their pre-test values. Record queries and output for the PR body.

## 2. Handler (test-first)

- [ ] 2.1 Test-first in `__tests__/webhooks.test.ts`: a pure `invoiceRecord(invoice)` returns `{ stripe_invoice_id, amount_paid: amount_paid / 100, currency, status (default "paid"), invoice_date: "YYYY-MM-DD" }`. Verify: fails (`invoiceRecord` not exported) via `npm run test:run -- webhooks`.
- [ ] 2.2 Add `invoiceRecord` to `lib/webhooks/stripe-handlers.ts` and make `recordInvoice` use it (D4). Verify: 2.1 passes and the existing `handleCheckoutCompleted` / `subscription_create` invoice tests still pass unchanged.
- [ ] 2.3 Reproduce D16 first. Add an `rpc` mock to the service-client mock and rewrite the renewal-path describe (`handleInvoicePaid — idempotencia y avance del puntero`) so each existing advance assertion (month + pointer, next rung, wrap + loop, fixed-term freeze, month before the last still advances, `program_variant_id` never written) reads the RPC arguments instead of the `update` payload, and add: (i) the renewal path makes **no** `invoices` upsert and **no** `subscriptions` update outside the RPC; (ii) the RPC receives `p_expected_months_elapsed` / `p_expected_content_ordinal` equal to the values read; (iii) `data: false` → resolves with no further writes; (iv) an RPC `error` → rejects; (v) on a fixed-term final month `stripe.subscriptions.update` is called **before** the RPC and `p_complete` is true; (vi) no position → all three `p_content_*` are null. Delete the misnamed "relanza si otra escritura concurrente se adelantó (no pierde el avance)" test. Verify: the new tests fail against current code for the stated reason (the separate upsert/update calls), shown in the test output.
- [ ] 2.4 Rewrite the renewal branch of `handleInvoicePaid` to one `record_invoice_and_advance` call via a `// keep:` cast on the client (rule 10, pattern of `lib/admin/dayActions.ts`); leave the Stripe cancellation call before it; rewrite the gate / update / `recordInvoice` docblock comments that describe the two-statement flow (D5). Verify: `npm run test:run -- webhooks` green; `grep -n '"Functions"\|Functions:' lib/supabase/types.ts` still shows `Record<string, never>`.

## 3. Verification and review

- [ ] 3.1 `grep -rn "recordInvoice\|handleInvoicePaid" docs/adr openspec/specs` and update any stale description (rule 20). Verify: grep output quoted in the PR.
- [ ] 3.2 Run `npx tsc --noEmit`, `npm run lint`, `npm run test:run`, `npm run build`. Verify: all four exit 0.
- [ ] 3.3 Runtime scope statement: an end-to-end renewal `invoice.paid` cannot be exercised non-destructively — the three Stripe-backed demo subscriptions have only their first (`subscription_create`) invoice, and a synthetic signed event would advance a demo client's month for real. Task 1.3 is the runtime verification of the new write; state this limit explicitly in the PR body rather than implying an end-to-end run.
- [ ] 3.4 Run `/security-review` (sensitive surface: webhook, money, service-role, migration); address or dismiss each finding with a reason in the PR body.

## 4. PR handoff

- [ ] 4.1 Open PR `fix(webhooks): record invoice and advance subscription atomically` from `task/atomic-invoice-paid`; body states when 023 was applied and verified (rule 11) with the 1.2/1.3 evidence, the 3.3 scope limit, the security-review outcome, and the silent-defect flag: **yes — migration, money (`months_elapsed` drives fixed-term completion and end of billing), service-role write path**. Squash-merge on green CI; then the separate `chore/archive-atomic-invoice-paid` PR, which also deletes D16's row and the Transactionality section's D16 sentence from `BACKLOG.md`.
